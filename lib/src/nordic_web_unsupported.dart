import 'dart:async';
import 'dart:typed_data';

class NordicDfuWeb {
  static Future startDfu(
      {required Uint8List uint8list,
      int dfuDelay = 15,
      onProgress,
      onComplete,
      onError,
      onLogs}) async {}
}
